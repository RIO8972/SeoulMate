# name: Deploy to EC2

# on:
#   push:
#     branches:
#       - main

# jobs:
#   build:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v3

#       - name: Set up JDK 17
#         uses: actions/setup-java@v3
#         with:
#           java-version: '17'
#           distribution: 'temurin'

#       # --- Gradle build (tests skip) ---
#       - name: Build oauthlogin with Gradle
#         run: |
#           cd back/oauthlogin
#           chmod +x ./gradlew
#           ./gradlew clean build -x test

#       - name: Build cityapi with Gradle
#         run: |
#           cd back/cityapi
#           chmod +x ./gradlew
#           ./gradlew clean build -x test

#       - name: Build contentapi with Gradle
#         run: |
#           cd back/contentapi
#           chmod +x ./gradlew
#           ./gradlew clean build -x test

#       # --- Docker Hub login ---
#       - name: Login to Docker Hub
#         uses: docker/login-action@v2
#         with:
#           username: ${{ secrets.DOCKER_USERNAME }}
#           password: ${{ secrets.DOCKER_PASSWORD }}

#       # --- Build & Push images ---
#       - name: Build and Push oauthlogin image
#         run: |
#           cd back/oauthlogin
#           docker build --no-cache -t rio8972/oauthlogin:latest .
#           docker push rio8972/oauthlogin:latest

#       - name: Build and Push cityapi image
#         run: |
#           cd back/cityapi
#           docker build --no-cache -t rio8972/cityapi:latest .
#           docker push rio8972/cityapi:latest

#       - name: Build and Push contentapi image
#         run: |
#           cd back/contentapi
#           docker build --no-cache -t rio8972/contentapi:latest .
#           docker push rio8972/contentapi:latest

#   deploy:
#     needs: build
#     runs-on: ubuntu-latest
#     steps:
#       - name: SSH into EC2 and deploy
#         uses: appleboy/ssh-action@master
#         with:
#           host: ${{ secrets.EC2_HOST }}
#           username: ubuntu
#           key: ${{ secrets.EC2_SSH_KEY }}
#           port: 22
#           script: |
#             echo "${{ secrets.DOCKER_PASSWORD }}" | docker login --username "${{ secrets.DOCKER_USERNAME }}" --password-stdin
#             cd docker-compose
#             docker-compose pull
#             docker-compose down
#             docker-compose up -d --force-recreate
#             docker image prune -a -f
